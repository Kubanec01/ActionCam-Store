export type AdventureBars = {
  id: number;
  image: string;
  title: string;
  description: string;
  delay: number;
};

export type CartProducts = {
  id: string;
  price: number;
  count: number;
};
